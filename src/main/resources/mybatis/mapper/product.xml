<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="products">
	<insert id="insertnewproduct" parameterType="product">
		<selectKey keyProperty="productCode" resultType="int">
			
		</selectKey>
		insert into product (productCode, productName, productDesc, productPrice, productImagePath, lowcategoryEng)
		values (#{productCode}, #{productName}, #{productDesc}, #{productPrice}, #{productImagePath}, #{lowcategoryEng})
	</insert>
	
	<delete id="deleteproduct" parameterType="int">
		delete from product where productCode=#{productCode}
	</delete>
</mapper>